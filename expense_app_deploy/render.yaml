services:
  - type: web
    name: voice-expense-app
    runtime: python

    buildCommand: "python -m py_compile server.py"
    startCommand: "python server.py"
    envVars:
      - key: NOTION_SECRET
        sync: false
